# 1316번 - 그룹 단어 체커

**난이도: Silver 5**

## 문제
그룹 단어란 단어에 존재하는 모든 문자에 대해서, 각 문자가 연속해서 나타나는 경우만을 말한다. 예를 들면, ccazzzzbb는 c, a, z, b가 모두 연속해서 나타나고, kin도 k, i, n이 연속해서 나타나기 때문에 그룹 단어이지만, aabbbccb는 b가 떨어져서 나타나기 때문에 그룹 단어가 아니다.
단어 N개를 입력으로 받아 그룹 단어의 개수를 출력하는 프로그램을 작성하시오.

## 입력
첫째 줄에 단어의 개수 N이 들어온다. N은 100보다 작거나 같은 자연수이다. 둘째 줄부터 N개의 줄에 단어가 들어온다. 단어는 알파벳 소문자로만 되어있고 중복되지 않으며, 길이는 최대 100이다.

## 출력
첫째 줄에 그룹 단어의 개수를 출력한다.

## 예제 입력 1
```
3
happy
new
year
```

## 예제 출력 1
```
3
```

## 예제 입력 2
```
4
aba
abab
abcabc
a
```

## 예제 출력 2
```
1
```

## 예제 입력 3
```
5
ab
aa
aca
ba
bb
```

## 예제 출력 3
```
4
```

## 예제 입력 4
```
2
yzyzy
zyzyz
```

## 예제 출력 4
```
0
```

## 예제 입력 5
```
1
z
```

## 예제 출력 5
```
1
```

## 예제 입력 6
```
9
aaa
aaazbz
babb
aazz
azbz
aabbaa
abacc
aba
zzaz
```

## 예제 출력 6
```
2
```

---

## 소스코드 (C#)
```cs
using System.Diagnostics;
using System.Text;

internal class Program
{
    public static StringBuilder sb;
    public static int a;
    public static bool[] visit;
    private static void Main(string[] args)
    {
        //현재 문자가 true인지 확인 후 true라면? -> 이전 문자열을 확인해서 같은 문자인지 확인한다. 
        int a = int.Parse(Console.ReadLine());
        int max = 0;
        //97 122
        for (int i = 0; i < a; i++)
        {
            bool[] result = new bool[26];
            string s = Console.ReadLine();
            bool isStop = false;
            for (int j = 0; j < s.Length; j++)
            {
                bool ab = result[(int)s[j] - 97];
                //해당 문자열이 방문이력이 있는지 확인을 한다.
                if (ab == true)
                {
                     bool abv = result[(int)s[j-1] - 97];
                    //같으면 okay 다르면 break; false               
                    if (s[j]!= s[j-1])
                    {
                        isStop = true;
                        break;
                    }
                }
                //처음이라면 해당 문자열을 방문이력 있음으로 변경한다.
                else
                {
                    //Console.WriteLine((int)s[j] - 97);
                    result[(int)s[j]-97] = true;
                }
            }
            if (!isStop) max++;
            isStop = false;
        }


        Console.WriteLine(max);
    }



}
```
